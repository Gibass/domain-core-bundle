# domain-core

PHP library for Symfony 6.4+ (including 7.x/8.x) providing base classes for Domain Architecture.

## Installation

```bash
composer require gibass/domain-core
```

Requirements: PHP 8.1+, Symfony 6.4+ (7.x/8.x supported).
